class makeItem {

  constructor(name,checked, id) {
    this.name = name;
    this.checked = checked;
    this.id = id;
  }

var ret = {
     "name" : name,
     "checked" : checked,
     "id" : id
   };

}
